package com.squaredcandy.europa.model.user

// Group type determines which groups are available to the user
enum class GroupType {
    // Only selected people access to this group
    Private,
    // Anyone can control this group
    Public,
}